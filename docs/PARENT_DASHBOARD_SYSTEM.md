# ระบบ Dashboard สำหรับผู้ปกครอง

## ภาพรวม

Dashboard แยกสำหรับผู้ปกครองที่สามารถเข้าดูข้อมูลบุตรหลานได้แบบ Read-only โดยไม่ต้องมีบัญชีในระบบหลัก

## URL Structure

```
/parent/login                    - หน้าล็อกอินผู้ปกครอง
/parent/verify?token=xxx         - ยืนยันอีเมลและตั้งรหัสผ่าน
/parent/dashboard                - Dashboard หลัก
/parent/dashboard/athlete/[id]   - ข้อมูลบุตรหลาน
/parent/dashboard/notifications  - การแจ้งเตือนทั้งหมด
/parent/dashboard/reports        - รายงานความก้าวหน้า
/parent/dashboard/settings       - ตั้งค่าการแจ้งเตือน
```

## Authentication Flow

### 1. การสร้างบัญชีผู้ปกครอง

```
นักกีฬาเพิ่มอีเมลผู้ปกครอง
    ↓
ระบบสร้าง parent_connections (is_verified = false)
    ↓
ส่งอีเมลพร้อม verification_token
    ↓
ผู้ปกครองคลิกลิงก์ /parent/verify?token=xxx
    ↓
ตั้งรหัสผ่าน (6 หลักขึ้นไป)
    ↓
สร้าง parent_users (เก็บ email + hashed_password)
    ↓
อัพเดท parent_connections (is_verified = true)
    ↓
Redirect ไป /parent/dashboard
```

### 2. การล็อกอิน

```
ผู้ปกครองเข้า /parent/login
    ↓
กรอก email + password
    ↓
ตรวจสอบ parent_users
    ↓
สร้าง session (JWT หรือ cookie)
    ↓
Redirect ไป /parent/dashboard
```

## Database Schema

### ตาราง parent_users

```sql
CREATE TABLE parent_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  
  -- Session management
  last_login_at TIMESTAMPTZ,
  login_count INTEGER DEFAULT 0,
  
  -- Security
  failed_login_attempts INTEGER DEFAULT 0,
  locked_until TIMESTAMPTZ,
  password_changed_at TIMESTAMPTZ DEFAULT NOW(),
  
  -- Status
  is_active BOOLEAN DEFAULT TRUE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_parent_users_email ON parent_users(email);
CREATE INDEX idx_parent_users_active ON parent_users(is_active);
```

### ตาราง parent_sessions

```sql
CREATE TABLE parent_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  parent_user_id UUID NOT NULL REFERENCES parent_users(id) ON DELETE CASCADE,
  
  -- Session data
  session_token TEXT UNIQUE NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  
  -- Device info
  user_agent TEXT,
  ip_address TEXT,
  device_fingerprint TEXT,
  
  -- Status
  is_active BOOLEAN DEFAULT TRUE,
  last_activity_at TIMESTAMPTZ DEFAULT NOW(),
  
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_parent_sessions_token ON parent_sessions(session_token);
CREATE INDEX idx_parent_sessions_user ON parent_sessions(parent_user_id);
CREATE INDEX idx_parent_sessions_expires ON parent_sessions(expires_at);
```

### อัพเดท parent_connections

```sql
ALTER TABLE parent_connections 
ADD COLUMN parent_user_id UUID REFERENCES parent_users(id) ON DELETE SET NULL;

CREATE INDEX idx_parent_connections_user ON parent_connections(parent_user_id);
```

## API Actions

### lib/parent-auth/actions.ts

```typescript
// ยืนยันอีเมลและตั้งรหัสผ่าน
verifyAndSetPassword(token: string, password: string)

// ล็อกอิน
parentLogin(email: string, password: string)

// ล็อกเอาท์
parentLogout()

// ตรวจสอบ session
getParentSession()

// เปลี่ยนรหัสผ่าน
changeParentPassword(oldPassword: string, newPassword: string)

// รีเซ็ตรหัสผ่าน
requestPasswordReset(email: string)
resetPassword(token: string, newPassword: string)
```

### lib/parent-dashboard/actions.ts

```typescript
// ดึงข้อมูลบุตรหลานทั้งหมด
getMyChildren()

// ดึงข้อมูลบุตรหลานคนเดียว
getChildDetails(athleteId: string)

// ดึงสถิติการเข้าฝึก
getChildAttendanceStats(athleteId: string, period: 'week' | 'month' | 'year')

// ดึงผลการทดสอบ
getChildPerformanceRecords(athleteId: string, limit?: number)

// ดึงเป้าหมาย
getChildGoals(athleteId: string)

// ดึงการแจ้งเตือน
getMyNotifications(limit?: number)

// ดึงรายงาน
getMyReports(athleteId?: string)
```

## UI Components

### 1. ParentLoginForm
- ฟอร์มล็อกอิน (email + password)
- Remember me checkbox
- ลิงก์ลืมรหัสผ่าน
- ลิงก์ติดต่อสโมสร

### 2. ParentVerifyForm
- ฟอร์มตั้งรหัสผ่าน
- Validation (6 หลักขึ้นไป)
- แสดงข้อมูลบุตรหลาน
- ยืนยันและสร้างบัญชี

### 3. ParentDashboard
- แสดงรายชื่อบุตรหลานทั้งหมด
- สถิติโดยรวม
- การแจ้งเตือนล่าสุด
- รายงานล่าสุด

### 4. ChildDetailView
- ข้อมูลส่วนตัว
- สถิติการเข้าฝึก
- ผลการทดสอบ
- เป้าหมาย
- กราฟความก้าวหน้า

### 5. ParentNotificationList
- รายการการแจ้งเตือนทั้งหมด
- Filter ตามประเภท
- แสดงสถานะอ่าน/ยังไม่อ่าน

### 6. ParentReportsList
- รายงานรายสัปดาห์/เดือน
- ดาวน์โหลด PDF
- แสดงกราฟ

## Security Features

### 1. Password Requirements
- อย่างน้อย 6 ตัวอักษร
- แนะนำให้มีตัวเลขและอักขระพิเศษ
- Hash ด้วย bcrypt

### 2. Session Management
- Session timeout: 7 วัน
- Auto-logout เมื่อไม่มีกิจกรรม 30 นาที
- จำกัด 3 sessions ต่อ user

### 3. Rate Limiting
- จำกัดการล็อกอิน: 5 ครั้ง/5 นาที
- Lock account 15 นาทีหลังล็อกอินผิด 5 ครั้ง

### 4. RLS Policies
- ผู้ปกครองเห็นเฉพาะข้อมูลบุตรหลานของตัวเอง
- Read-only ทั้งหมด (ไม่สามารถแก้ไขข้อมูลได้)

## Middleware

### parent-auth-middleware.ts

```typescript
// ตรวจสอบ session
// Redirect ไป /parent/login ถ้าไม่มี session
// Refresh session ถ้าใกล้หมดอายุ
```

## Email Templates

### 1. Verification Email
```
หัวข้อ: ยืนยันบัญชีผู้ปกครอง - [ชื่อสโมสร]

เรียน คุณ[ชื่อผู้ปกครอง]

[ชื่อนักกีฬา] ได้เพิ่มคุณเป็นผู้ปกครองในระบบ

คลิกลิงก์ด้านล่างเพื่อยืนยันอีเมลและตั้งรหัสผ่าน:
[ลิงก์ยืนยัน]

ลิงก์นี้จะหมดอายุใน 7 วัน

หลังจากยืนยันแล้ว คุณจะสามารถ:
- ดูสถิติการเข้าฝึกของบุตรหลาน
- ดูผลการทดสอบและความก้าวหน้า
- รับการแจ้งเตือนเมื่อมีเหตุการณ์สำคัญ
- ดูรายงานความก้าวหน้ารายสัปดาห์/เดือน

ขอบคุณครับ/ค่ะ
[ชื่อสโมสร]
```

### 2. Password Reset Email
```
หัวข้อ: รีเซ็ตรหัสผ่าน - [ชื่อสโมสร]

เรียน คุณ[ชื่อผู้ปกครอง]

คุณได้ขอรีเซ็ตรหัสผ่าน

คลิกลิงก์ด้านล่างเพื่อตั้งรหัสผ่านใหม่:
[ลิงก์รีเซ็ต]

ลิงก์นี้จะหมดอายุใน 1 ชั่วโมง

หากคุณไม่ได้ขอรีเซ็ตรหัสผ่าน กรุณาเพิกเฉยอีเมลนี้

ขอบคุณครับ/ค่ะ
[ชื่อสโมสร]
```

## Features

### Dashboard หลัก
- 📊 สถิติโดยรวมของบุตรหลานทั้งหมด
- 🔔 การแจ้งเตือนล่าสุด 5 รายการ
- 📈 กราฟการเข้าฝึกรายสัปดาห์
- 🎯 เป้าหมายที่กำลังดำเนินการ

### หน้าข้อมูลบุตรหลาน
- 👤 ข้อมูลส่วนตัว
- 📅 ตารางฝึกซ้อม
- ✅ ประวัติการเข้าฝึก (30 วันล่าสุด)
- 📊 ผลการทดสอบ (กราฟแสดงความก้าวหน้า)
- 🎯 เป้าหมายและความคืบหน้า
- 💬 ความคิดเห็นจากโค้ช

### หน้าการแจ้งเตือน
- 📬 รายการการแจ้งเตือนทั้งหมด
- 🔍 Filter ตามประเภท
- 📅 Filter ตามวันที่
- ✅ ทำเครื่องหมายอ่านแล้ว

### หน้ารายงาน
- 📄 รายงานรายสัปดาห์/เดือน
- 📥 ดาวน์โหลด PDF
- 📊 กราฟแสดงแนวโน้ม
- 💬 ความคิดเห็นจากโค้ช

### หน้าตั้งค่า
- 🔔 ตั้งค่าการแจ้งเตือน
- 🔐 เปลี่ยนรหัสผ่าน
- 👤 แก้ไขข้อมูลส่วนตัว
- 🚪 ล็อกเอาท์

## Mobile Responsive

- ✅ รองรับ Mobile, Tablet, Desktop
- ✅ Touch-friendly UI
- ✅ Bottom Navigation สำหรับ Mobile
- ✅ Swipe gestures

## Performance

- ✅ Server-side rendering
- ✅ Caching ข้อมูลที่ไม่เปลี่ยนบ่อย
- ✅ Lazy loading รูปภาพ
- ✅ Pagination สำหรับรายการยาว

## Analytics

### Metrics ที่ควรติดตาม
- จำนวนผู้ปกครองที่ล็อกอินต่อวัน
- จำนวนครั้งที่เข้าดูข้อมูล
- หน้าที่ดูบ่อยที่สุด
- อัตราการเปิดอ่านการแจ้งเตือน
- เวลาเฉลี่ยที่ใช้ในระบบ

## Future Enhancements

- [ ] Mobile App (React Native)
- [ ] Push Notifications
- [ ] การแชทกับโค้ช
- [ ] การชำระเงินออนไลน์
- [ ] ปฏิทินกิจกรรม
- [ ] อัลบั้มรูปภาพ
- [ ] วิดีโอไฮไลท์
- [ ] เปรียบเทียบผลงานกับเพื่อน

## Testing

### Unit Tests
- Authentication flow
- Password validation
- Session management

### Integration Tests
- Login/Logout flow
- Data fetching
- Notification display

### E2E Tests
- Complete user journey
- Mobile responsive
- Cross-browser compatibility

## Deployment

### Environment Variables
```env
PARENT_SESSION_SECRET=xxx
PARENT_SESSION_TIMEOUT=604800  # 7 days
PARENT_RATE_LIMIT_MAX=5
PARENT_RATE_LIMIT_WINDOW=300   # 5 minutes
```

### Database Migrations
```bash
./scripts/run-sql-via-api.sh scripts/92-create-parent-dashboard-system.sql
```

## Support

### FAQ สำหรับผู้ปกครอง
- วิธีการล็อกอิน
- ลืมรหัสผ่าน
- ไม่ได้รับอีเมลยืนยัน
- ข้อมูลไม่อัพเดท
- ติดต่อสโมสร

### ช่องทางติดต่อ
- อีเมล: support@yourclub.com
- โทร: 02-XXX-XXXX
- LINE: @yourclub

## สรุป

ระบบ Dashboard ผู้ปกครองช่วยให้:
- ✅ ผู้ปกครองติดตามความก้าวหน้าได้ง่าย
- ✅ ลดภาระงานของโค้ช (ไม่ต้องส่งรายงานด้วยตนเอง)
- ✅ เพิ่มความโปร่งใส
- ✅ สร้างความไว้วางใจ
- ✅ เพิ่มการมีส่วนร่วมของผู้ปกครอง
