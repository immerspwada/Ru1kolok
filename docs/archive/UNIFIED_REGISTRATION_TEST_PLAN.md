# Unified Registration Flow - Test Plan

## 🎯 วัตถุประสงค์
ทดสอบ end-to-end flow ของระบบสมัครสมาชิกแบบรวม (4 steps) และการอนุมัติจากโค้ช

## ✅ Prerequisites
- Database มี clubs และ coaches พร้อมใช้งาน
- RLS policies ทำงานถูกต้อง
- Middleware ทำงานถูกต้อง

---

## 📋 Test Scenarios

### Scenario 1: นักกีฬาสมัครสมาชิก (Happy Path)

#### Step 1: เข้าหน้าสมัคร
- URL: http://localhost:3000/register-membership
- ✅ ไม่ต้อง login ก่อน
- ✅ เห็น form 4 steps

#### Step 2: กรอกข้อมูลบัญชี (Step 1/4)
- Email: `newathlete@test.com`
- Password: `Athlete123!` (ต้องมีตัวพิมพ์ใหญ่)
- Confirm Password: `Athlete123!`
- ✅ Validation เป็นภาษาไทย
- ✅ กดปุ่ม "ถัดไป" → สร้างบัญชีสำเร็จ

#### Step 3: กรอกข้อมูลส่วนตัว (Step 2/4)
- ชื่อ-นามสกุล: `นักกีฬาใหม่ ทดสอบ`
- เบอร์โทร: `081-234-5678`
- ที่อยู่: `123 ถนนทดสอบ`
- ผู้ติดต่อฉุกเฉิน: `คุณแม่ - 089-999-9999`
- วันเกิด: `01/01/2005`
- กรุ๊ปเลือด: `O`
- ✅ กดปุ่ม "ถัดไป"

#### Step 4: อัปโหลดเอกสาร (Step 3/4)
- บัตรประชาชน: อัปโหลดรูป
- ทะเบียนบ้าน: อัปโหลดรูป
- สูติบัตร: อัปโหลดรูป
- ✅ กดปุ่ม "ถัดไป"

#### Step 5: เลือกชมรม (Step 4/4)
- ✅ เห็นรายการชมรมทั้งหมด
- ✅ มี search box
- ✅ แสดงจำนวนสมาชิกและโค้ช
- ✅ **ไม่มีการเลือกโค้ช** (ตาม AC1)
- เลือก: "ชมรมบาสเกตบอล"
- ✅ กดปุ่ม "ส่งใบสมัคร"

#### Expected Results:
```sql
-- membership_applications table
INSERT INTO membership_applications (
  user_id,
  club_id,           -- ✅ ชมรมที่เลือก
  status,            -- ✅ 'pending'
  assigned_coach_id, -- ✅ NULL (ยังไม่มี)
  personal_info,     -- ✅ JSONB data
  documents          -- ✅ Array of documents
);

-- profiles table
UPDATE profiles
SET membership_status = 'pending'  -- ✅
WHERE user_id = 'newathlete@test.com';
```

#### Step 6: Redirect หลังส่งใบสมัคร
- ✅ แสดง toast: "ส่งใบสมัครสำเร็จ! 🎉"
- ✅ Redirect → `/dashboard/athlete/applications`
- ✅ Middleware ตรวจสอบ: `membership_status = 'pending'`
- ✅ Redirect → `/pending-approval`
- ✅ เห็นหน้า "รอการอนุมัติ"

---

### Scenario 2: โค้ชเห็นและอนุมัติคำขอ

#### Step 1: โค้ช Login
- Email: `coach@test.com`
- Password: `Coach123!`
- ✅ Redirect → `/dashboard/coach`

#### Step 2: ไปหน้า Applications
- URL: `/dashboard/coach/applications`
- ✅ เห็นคำขอใหม่จาก "นักกีฬาใหม่ ทดสอบ"
- ✅ **เห็นเฉพาะชมรมตัวเอง** (RLS Policy)

#### Expected Query:
```sql
SELECT * FROM membership_applications
WHERE club_id = (
  SELECT club_id FROM profiles 
  WHERE user_id = auth.uid() AND role = 'coach'
)
AND status = 'pending';
```

#### Step 3: ดูรายละเอียดคำขอ
- ✅ เห็นข้อมูลส่วนตัว
- ✅ เห็นเอกสารแนบ (3 ไฟล์)
- ✅ เห็นชมรมที่สมัคร

#### Step 4: อนุมัติคำขอ
- กดปุ่ม "อนุมัติ"
- ✅ แสดง confirmation dialog

#### Expected Results:
```sql
-- 1. Update application
UPDATE membership_applications
SET 
  status = 'approved',
  assigned_coach_id = 'coach@test.com',  -- ✅ โค้ชที่อนุมัติ
  reviewed_by = 'coach@test.com',
  reviewed_at = NOW()
WHERE id = 'application_id';

-- 2. Create athlete profile
INSERT INTO athletes (
  user_id,
  club_id,
  coach_id,  -- ✅ โค้ชที่อนุมัติ
  ...
);

-- 3. Update profile
UPDATE profiles
SET 
  membership_status = 'active',  -- ✅
  club_id = 'basketball_club',
  coach_id = 'coach@test.com'
WHERE user_id = 'newathlete@test.com';
```

---

### Scenario 3: นักกีฬาเข้าใช้งานหลังอนุมัติ

#### Step 1: นักกีฬา Login อีกครั้ง
- Email: `newathlete@test.com`
- Password: `Athlete123!`

#### Step 2: Middleware Check
```typescript
// middleware.ts
if (profile.membership_status === 'active') {
  // ✅ Allow access to dashboard
  redirect('/dashboard/athlete');
}
```

#### Expected Results:
- ✅ เข้า `/dashboard/athlete` ได้
- ✅ เห็นข้อมูลโค้ชที่ดูแล
- ✅ เห็นตารางฝึก
- ✅ สามารถ check-in ได้

---

## 🧪 Verification Checklist

### Database Verification
```sql
-- 1. ตรวจสอบ membership_application
SELECT 
  id,
  user_id,
  club_id,           -- ต้องมีค่า
  status,            -- ต้องเป็น 'pending' หรือ 'approved'
  assigned_coach_id, -- NULL ถ้า pending, มีค่าถ้า approved
  personal_info,
  documents
FROM membership_applications
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'newathlete@test.com');

-- 2. ตรวจสอบ profile
SELECT 
  user_id,
  membership_status, -- ต้องเป็น 'pending' หรือ 'active'
  club_id,           -- NULL ถ้า pending, มีค่าถ้า active
  coach_id           -- NULL ถ้า pending, มีค่าถ้า active
FROM profiles
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'newathlete@test.com');

-- 3. ตรวจสอบ athlete profile (หลังอนุมัติ)
SELECT 
  id,
  user_id,
  club_id,
  coach_id
FROM athletes
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'newathlete@test.com');
```

### RLS Policy Verification
```sql
-- ตรวจสอบว่าโค้ชเห็นเฉพาะชมรมตัวเอง
-- Login as coach@test.com
SELECT * FROM membership_applications;
-- ต้องเห็นเฉพาะ applications ของชมรมตัวเอง

-- Login as athlete
SELECT * FROM membership_applications;
-- ต้องเห็นเฉพาะ applications ของตัวเอง
```

---

## ❌ Edge Cases to Test

### 1. นักกีฬาสมัครซ้ำ
- สมัครเสร็จแล้ว (status = pending)
- พยายามสมัครอีกครั้ง
- ✅ ต้องแสดง error: "มีคำขอรออนุมัติอยู่แล้ว"

### 2. โค้ชปฏิเสธคำขอ
- โค้ชกดปุ่ม "ปฏิเสธ"
- ✅ ต้องระบุเหตุผล (required)
- ✅ membership_status = 'rejected'
- ✅ นักกีฬาเห็นเหตุผล
- ✅ สามารถสมัครใหม่ได้

### 3. โค้ชพยายามอนุมัติคำขอของชมรมอื่น
- ✅ RLS Policy ต้องบล็อก
- ✅ ไม่เห็นคำขอของชมรมอื่น

---

## 🎯 Success Criteria

### ✅ Registration Flow
- [ ] สมัครได้ 4 steps ครบ
- [ ] สร้างบัญชีสำเร็จ
- [ ] เลือกชมรมได้ (ไม่เลือกโค้ช)
- [ ] บันทึก membership_application ถูกต้อง
- [ ] membership_status = 'pending'

### ✅ Coach Review Flow
- [ ] โค้ชเห็นเฉพาะคำขอของชมรมตัวเอง
- [ ] อนุมัติได้
- [ ] ปฏิเสธได้ (พร้อมเหตุผล)
- [ ] สร้าง athlete profile หลังอนุมัติ
- [ ] assigned_coach_id = โค้ชที่อนุมัติ

### ✅ Access Control
- [ ] Pending athlete ไม่เข้า dashboard ได้
- [ ] Active athlete เข้า dashboard ได้
- [ ] Rejected athlete เห็นเหตุผลและสมัครใหม่ได้

---

## 📝 Notes

- ทุก error message ต้องเป็นภาษาไทย ✅
- RLS policies ต้องทำงานถูกต้อง
- Middleware ต้องตรวจสอบ membership_status
- ไม่มีการเลือกโค้ชตอนสมัคร (ตาม AC1)

---

**สถานะ:** 📋 Ready for Testing  
**วันที่:** 23 พฤศจิกายน 2025
