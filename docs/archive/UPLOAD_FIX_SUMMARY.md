# แก้ไขปัญหาการอัปโหลดไฟล์ ✅

## ปัญหาที่พบ
ไม่สามารถอัปโหลดไฟล์ได้ในหน้า `/register-membership`

## สาเหตุ
Storage bucket `membership-documents` ยังไม่ได้ถูกสร้างใน Supabase

## การแก้ไข

### 1. สร้าง Storage Bucket
```bash
./scripts/run-sql-via-api.sh scripts/41-create-storage-bucket-simple.sql
```

**ผลลัพธ์:**
- ✅ Bucket `membership-documents` ถูกสร้างแล้ว
- ✅ ตั้งค่าเป็น public bucket
- ✅ จำกัดขนาดไฟล์ 5MB
- ✅ รองรับ JPG, PNG, PDF

### 2. ตั้งค่า RLS Policies
```bash
./scripts/run-sql-via-api.sh scripts/42-storage-rls-policies-safe.sql
```

**ผลลัพธ์:**
- ✅ สร้าง 6 policies หลัก
- ✅ Users สามารถอัปโหลด/ดู/แก้ไข/ลบไฟล์ของตัวเองได้
- ✅ Coaches สามารถดูไฟล์ของผู้สมัครในสโมสรได้
- ✅ Admins สามารถดูไฟล์ทั้งหมดได้

### 3. แก้ไข Component
แก้ไข `DocumentUpload.tsx` เพื่อตรวจสอบ `userId` ก่อนอัปโหลด:
- ✅ เพิ่มการตรวจสอบว่า userId มีค่าหรือไม่
- ✅ แสดงข้อความ error ที่ชัดเจน

## การทดสอบ

### ขั้นตอนการทดสอบ:
1. เปิด `http://localhost:3000/register-membership`
2. **ขั้นตอนที่ 1**: สร้างบัญชี (email + password)
3. **ขั้นตอนที่ 2**: กรอกข้อมูลส่วนตัว
4. **ขั้นตอนที่ 3**: อัปโหลดเอกสาร 3 ประเภท
   - บัตรประชาชน
   - ทะเบียนบ้าน
   - สูติบัตร
5. **ขั้นตอนที่ 4**: เลือกกีฬา
6. กดส่งใบสมัคร

### ผลลัพธ์ที่คาดหวัง:
- ✅ อัปโหลดไฟล์สำเร็จ
- ✅ แสดง preview ของไฟล์
- ✅ แสดง progress bar ขณะอัปโหลด
- ✅ สามารถลบและอัปโหลดใหม่ได้

## การตรวจสอบ

ตรวจสอบว่า storage ตั้งค่าถูกต้อง:
```bash
./scripts/run-sql-via-api.sh scripts/verify-storage-setup.sql
```

ผลลัพธ์:
```json
{
  "bucket_exists": 1,
  "policy_count": 9
}
```

## ไฟล์ที่เกี่ยวข้อง

### Scripts
- `scripts/41-create-storage-bucket-simple.sql` - สร้าง bucket
- `scripts/42-storage-rls-policies-safe.sql` - สร้าง RLS policies
- `scripts/verify-storage-setup.sql` - ตรวจสอบการตั้งค่า

### Components
- `components/membership/DocumentUpload.tsx` - Component อัปโหลดไฟล์
- `components/membership/RegistrationForm.tsx` - Form หลัก
- `lib/membership/storage.ts` - Logic การอัปโหลด

### Documentation
- `docs/STORAGE_SETUP_COMPLETE.md` - รายละเอียดการตั้งค่า
- `docs/STORAGE_BUCKET_SETUP.md` - คู่มือการตั้งค่าแบบ manual

## หมายเหตุ

- ไฟล์จะถูกเก็บในรูปแบบ: `{userId}/{documentType}_{timestamp}.{ext}`
- RLS policies ป้องกันไม่ให้ผู้ใช้เข้าถึงไฟล์ของคนอื่น
- ขนาดไฟล์สูงสุด 5MB ต่อไฟล์
- รองรับเฉพาะ JPG, PNG, PDF

## สถานะ

🟢 **แก้ไขเสร็จสมบูรณ์** - ระบบพร้อมใช้งาน
