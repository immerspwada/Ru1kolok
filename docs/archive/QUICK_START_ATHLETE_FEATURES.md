# 🚀 Quick Start: ระบบแจ้งเตือนและการลาสำหรับนักกีฬา

## ⚡ การติดตั้งด่วน (5 นาที)

### ขั้นตอนที่ 1: รัน Database Migrations

```bash
cd sports-club-management

# 1. ติดตั้งระบบแจ้งเตือน
./scripts/run-sql-via-api.sh scripts/70-fix-notifications.sql
./scripts/run-sql-via-api.sh scripts/70-notification-triggers.sql

# 2. ปรับปรุงระบบลา
./scripts/run-sql-via-api.sh scripts/71-enhance-leave-requests.sql
```

### ขั้นตอนที่ 2: ตั้งค่า Cron Job (Optional แต่แนะนำ)

สำหรับการเตือนก่อนฝึก 1 ชั่วโมง:

```bash
# รัน script นี้
./scripts/run-sql-via-api.sh scripts/setup-notification-cron.sql
```

**หมายเหตุ:** ถ้าเจอ error เกี่ยวกับ pg_cron:
1. ไปที่ Supabase Dashboard → Database → Extensions
2. เปิดใช้งาน `pg_cron`
3. รัน script อีกครั้ง

### ขั้นตอนที่ 3: ทดสอบระบบ

```bash
# ทดสอบว่าระบบทำงานถูกต้อง
./scripts/run-sql-via-api.sh scripts/test-notifications.sql
```

## ✅ เสร็จแล้ว!

ระบบพร้อมใช้งาน นักกีฬาจะได้รับการแจ้งเตือนอัตโนมัติเมื่อ:
- ✅ โค้ชสร้างตารางฝึกใหม่
- ✅ โค้ชสร้างประกาศใหม่
- ✅ ก่อนเวลาฝึก 1 ชั่วโมง (ถ้าตั้งค่า Cron)
- ✅ โค้ชบันทึกผลการทดสอบ
- ✅ โค้ชอนุมัติ/ปฏิเสธคำขอลา

## 📱 หน้าจอใหม่

### สำหรับนักกีฬา

1. **หน้าการแจ้งเตือน**
   - URL: `/dashboard/athlete/notifications`
   - ดูการแจ้งเตือนทั้งหมด
   - ทำเครื่องหมายว่าอ่านแล้ว/ลบ

2. **หน้าประวัติการลา**
   - URL: `/dashboard/athlete/leave-history`
   - ดูประวัติการลาทั้งหมด
   - Filter ตามสถานะ

## 🎯 การใช้งานด่วน

### นักกีฬา: ดูการแจ้งเตือน
1. ไปที่ `/dashboard/athlete/notifications`
2. คลิกการแจ้งเตือนเพื่อดูรายละเอียด
3. ทำเครื่องหมายว่าอ่านแล้วหรือลบ

### นักกีฬา: แจ้งลาการฝึก
1. ไปที่หน้าตารางฝึก
2. คลิก "แจ้งลา" ในตารางที่ต้องการ
3. กรอกเหตุผล (อย่างน้อย 10 ตัวอักษร)
4. ส่งคำขอ
5. รอการอนุมัติจากโค้ช

### นักกีฬา: ดูประวัติการลา
1. ไปที่ `/dashboard/athlete/leave-history`
2. ดูสถานะและความเห็นจากโค้ช

## 🔍 การตรวจสอบ

### ตรวจสอบว่าระบบทำงาน

```sql
-- ดูการแจ้งเตือนล่าสุด
SELECT type, title, message, created_at 
FROM notifications 
ORDER BY created_at DESC 
LIMIT 10;

-- ดูสถิติการแจ้งเตือน
SELECT type, count(*) 
FROM notifications 
GROUP BY type;

-- ตรวจสอบ Cron Job
SELECT * FROM cron.job WHERE jobname = 'session-reminders';
```

## 📚 เอกสารเพิ่มเติม

- **เอกสารฉบับเต็ม:** `docs/ATHLETE_NOTIFICATIONS_LEAVE_SYSTEM.md`
- **สรุปการพัฒนา:** `ATHLETE_FEATURES_IMPLEMENTATION.md`

## 🆘 Troubleshooting

### การแจ้งเตือนไม่ทำงาน?

```sql
-- ตรวจสอบ Triggers
SELECT * FROM pg_trigger WHERE tgname LIKE '%notify%';

-- ตรวจสอบ RLS Policies
SELECT * FROM pg_policies WHERE tablename = 'notifications';
```

### การเตือนก่อนฝึกไม่ทำงาน?

1. ตรวจสอบว่าเปิด pg_cron แล้ว
2. ตรวจสอบ Cron Job:
```sql
SELECT * FROM cron.job WHERE jobname = 'session-reminders';
```

3. ทดสอบฟังก์ชัน:
```sql
SELECT send_session_reminders();
```

## 💡 Tips

- การแจ้งเตือนจะอัพเดททุก 30 วินาที
- นักกีฬาต้องแจ้งลาล่วงหน้าอย่างน้อย 2 ชั่วโมง
- เหตุผลการลาต้องมีอย่างน้อย 10 ตัวอักษร
- Cron Job จะตรวจสอบทุก 5 นาที

## 🎉 พร้อมใช้งาน!

ระบบพร้อมแล้ว ลองสร้างตารางฝึกใหม่หรือประกาศใหม่เพื่อดูการแจ้งเตือนทำงาน!
