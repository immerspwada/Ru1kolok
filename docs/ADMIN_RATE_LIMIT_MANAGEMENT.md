# คู่มือการจัดการ Rate Limiting สำหรับ Admin

## ภาพรวม

ระบบ Rate Limiting Management ช่วยให้ Admin สามารถ:
- ✅ ตรวจสอบสถานะการสมัครสมาชิก
- ✅ ดูว่ามีผู้ใช้ถูก rate limit หรือไม่
- ✅ สร้างบัญชีให้ผู้ใช้โดยไม่ถูก rate limit (ปลดล็อก)
- ✅ ติดตามกิจกรรมการสมัครล่าสุด

## การเข้าถึง

**URL:** `/dashboard/admin/rate-limits`

**สิทธิ์:** เฉพาะ Admin เท่านั้น

**เมนู:** Dashboard → Admin → จัดการ Rate Limiting

---

## ฟีเจอร์หลัก

### 1. แดชบอร์ดสถิติ

แสดงข้อมูล 3 ส่วน:

#### 📊 การสมัครในชั่วโมงที่แล้ว
- แสดงจำนวนการสมัครใน 1 ชั่วโมงที่ผ่านมา
- ⚠️ ถ้าเกิน 5 ครั้ง = อาจมีผู้ใช้ถูก rate limit
- ✅ ถ้าน้อยกว่า 5 ครั้ง = ปกติ

#### 📈 การสมัครใน 24 ชั่วโมง
- แสดงจำนวนการสมัครทั้งหมดใน 1 วัน
- ใช้ติดตามแนวโน้มการสมัคร

#### 🔒 สถานะระบบ
- **ปกติ** (สีเขียว): ไม่มีปัญหา rate limiting
- **อาจถูก rate limit** (สีแดง): มีการสมัครบ่อยเกินไป

---

### 2. สร้างผู้ใช้ใหม่ (ปลดล็อก Rate Limit)

**วิธีใช้งาน:**

1. **เมื่อผู้ใช้แจ้งว่าถูก rate limit:**
   - ผู้ใช้จะเห็นข้อความ: "ลองสมัครบ่อยเกินไป กรุณารอสักครู่..."
   - ติดต่อ Admin เพื่อขอความช่วยเหลือ

2. **Admin ดำเนินการ:**
   ```
   ① ไปที่หน้า /dashboard/admin/rate-limits
   ② กรอกข้อมูลในฟอร์ม "สร้างผู้ใช้ใหม่"
      - อีเมล: อีเมลของผู้ใช้
      - ชื่อ-นามสกุล: ชื่อเต็มของผู้ใช้
      - รหัสผ่าน: (ค่าเริ่มต้น TestPassword123!)
   ③ คลิก "สร้างผู้ใช้ (Bypass Rate Limit)"
   ④ ส่งข้อมูล login ให้ผู้ใช้
   ```

3. **ผู้ใช้ได้รับข้อมูล:**
   - Email: xxx@example.com
   - Password: TestPassword123!
   - แนะนำให้เปลี่ยนรหัสผ่านหลัง login

**ข้อดี:**
- ✅ ไม่ถูก rate limit (ใช้ Admin API)
- ✅ สร้างได้ทันที ไม่ต้องรอ
- ✅ บัญชีพร้อมใช้งานทันที

---

### 3. การสมัครล่าสุด

แสดงรายการ 20 บัญชีล่าสุด:
- อีเมล
- เวลาที่สมัคร (แบบ relative: "5 นาทีที่แล้ว")
- วันที่-เวลาแบบเต็ม

**ประโยชน์:**
- ตรวจสอบว่ามีการสมัครบ่อยเกินไปหรือไม่
- ดูแพทเทิร์นการสมัคร
- ระบุ IP ที่อาจมีปัญหา

---

## สถานการณ์การใช้งาน

### สถานการณ์ที่ 1: ผู้ใช้ถูก Rate Limit

**ปัญหา:**
```
ผู้ใช้: "ฉันลองสมัครหลายครั้งแล้ว แต่ระบบบอกว่า 
       'ลองสมัครบ่อยเกินไป กรุณารอสักครู่'"
```

**วิธีแก้:**
1. ไปที่ `/dashboard/admin/rate-limits`
2. ตรวจสอบสถิติ "การสมัครในชั่วโมงที่แล้ว"
3. ใช้ฟอร์ม "สร้างผู้ใช้ใหม่" สร้างบัญชีให้
4. ส่งข้อมูล login ให้ผู้ใช้

**ผลลัพธ์:**
- ✅ ผู้ใช้ได้บัญชีทันที
- ✅ ไม่ต้องรอ 1-2 ชั่วโมง

---

### สถานการณ์ที่ 2: ตรวจสอบการสมัครผิดปกติ

**ปัญหา:**
```
สงสัยว่ามีคนพยายาม spam การสมัคร
```

**วิธีตรวจสอบ:**
1. ดูสถิติ "การสมัครในชั่วโมงที่แล้ว"
   - ถ้าเกิน 10 ครั้ง = ผิดปกติ
2. ดูรายการ "การสมัครล่าสุด"
   - ตรวจสอบว่ามีอีเมลแปลกๆ หรือไม่
   - ดูว่ามีการสมัครติดกันหลายครั้งหรือไม่

**การดำเนินการ:**
- ถ้าเป็น spam: ลบบัญชีที่สงสัย
- ถ้าเป็นผู้ใช้จริง: ปล่อยให้ rate limit ทำงาน

---

### สถานการณ์ที่ 3: สร้างบัญชีจำนวนมากสำหรับทดสอบ

**ปัญหา:**
```
ต้องการสร้างบัญชีทดสอบหลายบัญชี
แต่ถูก rate limit
```

**วิธีแก้:**
1. ใช้ฟอร์ม "สร้างผู้ใช้ใหม่"
2. สร้างทีละบัญชี (ไม่ถูก rate limit)
3. หรือใช้สคริปต์:
   ```bash
   node scripts/create-test-user-via-api.js
   ```

---

## API Endpoint

### POST `/api/admin/create-user`

**สิทธิ์:** Admin only

**Request Body:**
```json
{
  "email": "user@example.com",
  "password": "TestPassword123!",
  "full_name": "ชื่อ นามสกุล"
}
```

**Response (Success):**
```json
{
  "success": true,
  "user": {
    "id": "uuid",
    "email": "user@example.com"
  },
  "message": "User created successfully"
}
```

**Response (Error):**
```json
{
  "error": "Error message"
}
```

**การทำงาน:**
1. ตรวจสอบว่าผู้เรียกใช้เป็น Admin
2. ใช้ `supabase.auth.admin.createUser()` (bypass rate limit)
3. สร้าง profile และ athlete record
4. Return ข้อมูล user

---

## คำถามที่พบบ่อย (FAQ)

### Q1: ทำไมต้องมี Rate Limiting?
**A:** เพื่อป้องกัน:
- Spam accounts
- Brute force attacks
- Bot registration
- Abuse ระบบ

### Q2: Rate Limit จะหมดอายุเมื่อไหร่?
**A:** โดยปกติ 1-2 ชั่วโมง หลังจากการสมัครครั้งแรก

### Q3: สามารถปิด Rate Limiting ได้ไหม?
**A:** ไม่แนะนำ! Rate limiting เป็น security feature ที่สำคัญ
- ใน Development: ใช้ Admin API สร้าง user แทน
- ใน Production: ควรเปิดไว้เสมอ

### Q4: ถ้าผู้ใช้ลืมรหัสผ่านที่ Admin สร้างให้?
**A:** ผู้ใช้สามารถ:
1. ใช้ฟีเจอร์ "ลืมรหัสผ่าน" ในหน้า login
2. หรือติดต่อ Admin เพื่อ reset รหัสผ่าน

### Q5: จำนวนการสมัครเท่าไหร่ถึงจะถูก rate limit?
**A:** 
- Supabase default: 3-5 ครั้งต่อชั่วโมงต่อ IP
- ไม่สามารถปรับได้ (เป็น Supabase policy)

### Q6: ถ้าสร้าง user ผ่าน Admin แล้วเกิด error?
**A:** ตรวจสอบ:
1. อีเมลซ้ำหรือไม่ (ต้องไม่ซ้ำ)
2. รหัสผ่านถูกต้องหรือไม่ (ต้องมีอย่างน้อย 8 ตัวอักษร)
3. มี club ในระบบหรือไม่ (ต้องมีอย่างน้อย 1 club)

---

## Best Practices

### ✅ ควรทำ:
1. ตรวจสอบสถิติเป็นประจำ
2. ช่วยเหลือผู้ใช้ที่ถูก rate limit ทันที
3. บันทึกข้อมูล login ที่สร้างให้ผู้ใช้
4. แนะนำให้ผู้ใช้เปลี่ยนรหัสผ่าน

### ❌ ไม่ควรทำ:
1. สร้างบัญชีให้ผู้ใช้โดยไม่ได้รับการร้องขอ
2. ใช้รหัสผ่านที่ง่ายเกินไป
3. แชร์รหัสผ่านผ่านช่องทางที่ไม่ปลอดภัย
4. ปิด rate limiting ใน production

---

## การติดตาม (Monitoring)

### ตัวชี้วัดที่ควรติดตาม:

1. **การสมัครต่อชั่วโมง**
   - ปกติ: 0-5 ครั้ง
   - ผิดปกติ: > 10 ครั้ง

2. **การสมัครต่อวัน**
   - ปกติ: 10-50 ครั้ง (ขึ้นอยู่กับขนาดองค์กร)
   - ผิดปกติ: > 100 ครั้ง

3. **จำนวนผู้ใช้ที่ถูก rate limit**
   - ควรน้อยกว่า 5% ของการสมัครทั้งหมด
   - ถ้ามากกว่า = อาจมีปัญหากับ UX

---

## สรุป

ระบบ Rate Limit Management ช่วยให้ Admin:
- ✅ ช่วยเหลือผู้ใช้ที่ถูกบล็อกได้ทันที
- ✅ ตรวจสอบและติดตามการสมัครได้
- ✅ รักษาความปลอดภัยของระบบ
- ✅ สร้างประสบการณ์ที่ดีให้ผู้ใช้

**หมายเหตุ:** ฟีเจอร์นี้ใช้ Admin API ของ Supabase ซึ่งต้องการ service role key ดังนั้นต้องระมัดระวังในการใช้งาน
